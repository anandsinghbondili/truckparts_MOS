name: truckparts_mos
description: "Truck Parts - MOS - Mobile Ordering System for truck parts and components."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.9.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dpendencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # State Management
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  bloc: ^8.1.4
  provider: ^6.1.2

  # Navigation
  go_router: ^14.6.2

  # HTTP & API
  dio: ^5.7.0
  retrofit: ^4.4.1
  json_annotation: ^4.9.0

  # Local Storage & Security
  shared_preferences: ^2.3.2
  flutter_secure_storage: ^9.2.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # App Information
  package_info_plus: ^8.0.0

  # Dependency Injection
  get_it: ^8.0.0
  injectable: ^2.5.0

  # UI & Icons
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.15
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0

  # Utils
  intl: ^0.19.0
  uuid: ^4.5.1
  connectivity_plus: ^6.1.0
  permission_handler: ^11.3.1
  url_launcher: ^6.3.0

  # Video Player
  youtube_player_iframe: ^5.2.0

  # Image Picker & Camera
  image_picker: ^1.1.2
  camera: ^0.10.5+5
  image: ^4.1.3

  # ML Kit for Text Recognition
  google_mlkit_text_recognition: ^0.11.0

  # Share functionality
  share_plus: ^7.2.1

  # Form Validation
  formz: ^0.7.0
  dartz: ^0.10.1
  flutter_spinkit: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Code Generation
  build_runner: ^2.4.13
  injectable_generator: ^2.6.2
  retrofit_generator: ^8.2.1
  json_serializable: ^6.8.0
  hive_generator: ^2.0.1

  # Linting
  flutter_lints: ^5.0.0

  # Testing
  bloc_test: ^9.1.7
  mocktail: ^1.0.4
  integration_test:
    sdk: flutter

  # Launcher Icons
  flutter_launcher_icons: ^0.13.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Assets
  assets:
    - assets/images/
    - assets/images/brand_logos/
    - assets/images/brand_logos/BANCO.png
    - assets/images/brand_logos/GOETZE.png
    - assets/images/brand_logos/JMP.png
    - assets/images/brand_logos/KAR VALVES.png
    - assets/images/brand_logos/KSPG.png
    - assets/images/brand_logos/LEYPARTS.png
    - assets/images/brand_logos/MAHLE.png
    - assets/images/brand_logos/RBL.png
    - assets/images/brand_logos/SETCO.png
    - assets/images/brand_logos/SPICER.png
    - assets/images/brand_logos/SVL.png
    - assets/images/brand_logos/TALBROS QHT.png
    - assets/images/brand_logos/TALBROS.png
    - assets/images/brand_logos/TIGER POWER.png
    - assets/images/brand_logos/USHA.png
    - assets/icons/
    - assets/logos/
    - assets/data/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Flutter Launcher Icons Configuration
flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true # Remove alpha channel for App Store compliance
  # Use padded icon variation (512x512 with 15% padding) for proper display
  image_path: "assets/icons/TP_Icon_Padded_v2.png"
  # Android adaptive icon configuration
  adaptive_icon_background: "#374151"
  adaptive_icon_foreground: "assets/icons/TP_Icon_Padded_v2.png"
  min_sdk_android: 21
